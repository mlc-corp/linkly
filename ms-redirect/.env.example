# App
PORT=8080

# AWS & DynamoDB
AWS_REGION=us-east-1
DDB_TABLE=LinklyTable

# LOCAL usar dynamodb-local (compose)
# En local, deja esta línea activa:
DDB_ENDPOINT=http://localhost:8000

# STAGING/PROD en AWS:
# - NO definas DDB_ENDPOINT (comentado o eliminado).
# - Usa IAM Role de la tarea (no se requiere access key/secret).
# - Si quieres correr local apuntando a AWS (no recomendado), entonces quita DDB_ENDPOINT y configura credenciales con AWS CLI.
#
# Credenciales fake útiles SOLO para dynamodb-local:
AWS_ACCESS_KEY_ID=fake
AWS_SECRET_ACCESS_KEY=fake

# Para E2E apuntar a staging (opcional):
# APP_BASE_URL=https://<tu-load-balancer-o-dominio-de-staging>
